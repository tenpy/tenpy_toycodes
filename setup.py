# this file allows to install toycodes of this repository, locally with
#     pip install -e .
# or online with
#     pip install git+https://github.com/tenpy/tenpy_toycodes.git
# to uninstall, simply
#     pip uninstall tenpy-toycodes

from setuptools import setup

setup(name='tenpy-toycodes',
      version='0.1.1',
      packages=['tenpy_toycodes'])
